<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ScrapDAO">

<!-- list -->
<select id="getScrapList" parameterType="scrap" resultType="scrap">
SELECT * FROM SCRAP ORDER BY SCRAP_ID DESC
</select>

<!-- check Scrap -->
<select id="chkScrap" parameterType="scrap" resultType="scrap">
SELECT * FROM SCRAP WHERE userId = #{userId} and ad_id = #{ad_id}
</select>

<!-- insert -->
<insert id="insertScrap" parameterType="scrap">
INSERT INTO SCRAP (SCRAP_ID, USERID, NAME, COMPANY_ID, COMPANY_NAME, AD_ID, AD_TITLE, SCRAP_DATE)
VALUES (scrap_id_seq.NEXTVAL, #{userId}, #{name}, #{company_id},#{company_name}, #{ad_id}, #{ad_title}, SYSDATE)
</insert>

<!-- delete -->
<select id="deleteScrap" parameterType="int">
DELETE * FROM SCRAP WHERE SCRAP_ID = #{scrap_id}
</select>

</mapper>
  


